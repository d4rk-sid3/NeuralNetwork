#pragma once
#include <iostream>
#include <fstream>
#include <sstream>
#include <vector>
#include <map>

enum NeuronType {
    PERCEPTRON,
    SIGMOID,
};

enum ActivationFunctionType {
    SIGMOID,
    RELU,
    TANH
};
